# HistoryJuno 验证合同

当前开发版本：`0.4.1`。

## 验证原则

- 每项现行需求必须对应至少一种可重复的验收方式。
- 命令的权威声明位于根目录 project.manifest.json。
- 未执行、失败和不适用必须明确区分。

## 验证层级

| 编号 | 层级 | 适用时机 | 命令或步骤 | 通过标准 |
| --- | --- | --- | --- | --- |
| VERIFY-FAST | 快速检查 | 每次相关修改后 | dotnet build .\b-Code\HistoryJuno\HistoryJuno.csproj -c Release | 0 警告、0 错误 |
| VERIFY-UNIT | 自动验证 | 账号、导入、页面协议或进程运行器修改后 | dotnet run --project .\b-Code\HistoryJuno.Tests\HistoryJuno.Tests.csproj -c Release | 11 组全部 PASS，0 警告、0 错误；覆盖筛选、导入编排、全局代理、脱敏、WSL/compose、脚本超时与端口探测 |
| VERIFY-FULL | 完整回归 | 合并或里程碑收口前 | powershell -File .\b-Code\Test-ProjectContract.ps1 -Instantiation | 项目合同和占位符检查通过 |
| VERIFY-ACCEPT | 交付验收 | 宿主提交后 | 确认仅有 Juno 顶层页，通过视图选择切换账号管理、服务状态、账号导入子页，筛选账号，选择 JSON/分组并导入，再执行代理重连和刷新 | 启动单实例且 8080/9090 就绪；筛选联动；导入出现确认，新账号绑定所选分组和唯一全局代理；服务页四行；停止动作出现确认 |

## 环境前提

- 操作系统或设备：Windows 10/11。
- 工具与版本：.NET SDK 9、目标框架 net8.0、HistoryVulcan 5.1.2、HistoryAurora 1.9.2。
- 外部服务或材料：本机 Sub2API 脚本目录；账号实值验收需 Sub2API 已由用户启动；端口未监听时
  服务状态应显示“未连接”，账号读取返回脱敏的启动提示。
- 导入实值验收需准备不含生产凭据的专用 JSON，并保证活动代理列表只有一个不同的 `ip_address`；
  自动测试只使用内存中的模拟令牌和账号，不写真实 Sub2API 数据。

## 证据保存

| 证据类型 | 保存位置 | 是否入库 | 保留规则 |
| --- | --- | --- | --- |
| 构建日志 | 本次命令输出 | 否 | 保留最近一次结果 |
| 宿主验收记录 | b-Office/current/项目概览.md | 是 | 每次模块版本更新时更新 |


## VERIFY-SCENE-REG

已通过：Juno `sub2api` 页面声明 `scene=HistoryJuno`；提交后的实例已热重载并附着，其他场景布局保持独立。
