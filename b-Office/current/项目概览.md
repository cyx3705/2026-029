# HistoryJuno 项目概览

更新日期：2026-09-13

## 一句话定义

HistoryJuno 0.4.0 是注册到 HistoryVulcan/Aurora 的 Sub2API 账号与服务控制面板。

## 目标与边界

### 目标

- 在 Aurora 页面提供启动、代理连接、关闭和状态刷新操作。
- 默认展示 OpenAI OAuth 账号管理与 5h/7d 已用额度，并支持按分组、状态检索。
- 从 JSON 来源导入账号，选择目标分组，并自动绑定本机全局唯一代理 IP。
- 复用现有 Sub2API PowerShell 工具，不引入第二套服务实现。

### 范围

- HistoryVulcan 模块命令、Aurora 描述化页面和动作声明。
- 本机 Sub2API 管理登录、OpenAI OAuth 账号分页查询和批量额度只读查询。
- Sub2API 分组/代理查询、账号导入和导入后分组/代理写回。
- 本机 8080、9090、7890、17890 端口状态探测。

### 非目标

- 不重写 Sub2API 服务本体或代理内核。
- 不创建独立网页或自建 WPF 控件。

## 当前里程碑

M4：0.4.0 将账号导入并入 Juno 子页，Release 构建及 11 组自动验证通过，待模块提交。

| 状态 | 内容 |
| --- | --- |
| 已完成 | Juno 单顶层页与账号管理/服务状态/账号导入三个子页、分组/状态检索、全局代理导入编排和自动验证已实现 |
| 进行中 | 0.4.0 构建验证与宿主模块提交前检查 |
| 下一步 | 由宿主执行 vulcan.dev.submit 并等待用户审批 |

## 主要交付物

| 交付物 | 权威位置 | 完成判据 |
| --- | --- | --- |
| HistoryJuno 模块 | b-Code/HistoryJuno | Release 构建通过，Aurora 页面协议可解析 |
| 自动验证 | b-Code/HistoryJuno.Tests | 页面、筛选、导入、代理、额度、分页和脱敏检查全部通过 |

## 关键约束

- 平台或环境：Windows、PowerShell、.NET 8。
- 兼容性：HistoryVulcan 5.1.2，HistoryAurora 1.9.2；来源选择使用 sourcePicker，动态分组使用 optionsSource。
- 安全与权限：关闭动作由宿主确认；管理凭据只在内存中读取，不进入日志、命令结果或页面数据。
- 时间、成本或资源：不常驻后台线程，仅在点击动作或刷新时运行探测。

## 外部依赖

| 依赖 | 固定版本或位置 | 用途 | 权威文档 |
| --- | --- | --- | --- |
| HistoryVulcan.Core | C:\OneHistory\HistoryClio\2026-023-HistoryVulcan\z-Publish | 模块命令总线契约 | HistoryVulcan API 手册 |
| Sub2API scripts | %USERPROFILE%\Desktop\easyTOOL\SU2API | 启停服务与重连代理 | 本机工具目录 |
| Sub2API admin API | http://127.0.0.1:8080 | 账号、分组、代理、导入与 5h/7d 额度 | Sub2API 管理接口 |

## 已知问题与最近验证

| 编号或日期 | 类型 | 结果或影响 | 关闭条件或证据 |
| --- | --- | --- | --- |
| ISSUE-001 | 环境限制 | F 盘工作树默认 obj 无法创建 | manifest 构建命令改用 C 盘 .build 输出 |
| 2026-08-31 | 最近验证 | Release 构建 0 警告、0 错误 | dotnet build 输出 |
| 2026-08-31 | M3 自动验证 | 11 组页面、筛选、导入、代理、额度和脱敏检查通过 | project.manifest.json 的 test 命令 |

| 2026-09-13 | V0.4 验证 | Release 构建 0 警告、0 错误；11 组自动检查通过；保留 0.3.1 来源回写修复 | 本轮命令输出 |
| 2026-09-13 | VERIFY-FULL 未通过 | 合同转发脚本依赖 F 盘不存在的相邻 Diana 源码目录；未跨仓运行 | 修复共享合同的工作区可用性后重验 |


## 场景页面注册（0.4.1）

REQ-SCENE-REG：页面显式声明 `scene=HistoryJuno`，注册初值只用于本模块场景。用户保存的各场景完整布局（包括分栏、位置、比例和隐藏状态）优先；模块刷新不得主动打开其他场景的页面。需要 Aurora 1.21.1 的场景注册隔离。
